# mastergo-mcp 配置模板（复制为 .env 后填写真实值；.env 已加入 .gitignore，不会提交）
# 优先级：命令行参数 > 环境变量 > .env
#
# 本项目完全基于 MasterGo 网页接口自研（不依赖官方 MCP 网关），仅需浏览器 Cookie：
# 登录 mastergo.com 后，打开开发者工具 → Network → 任意请求的 Request Headers 复制整段 Cookie
MG_COOKIE="gfsessionid=xxx; ..."

# API 基础地址（默认 https://mastergo.com，一般无需修改）
# MG_BASE_URL=https://mastergo.com